Algoritmo HelloWorld;

PARA_NAO_ESCREVER_TUDO_DE_NOVO max(a,b: SemCasaDecimal) : SemCasaDecimal;
:)
  
:(;
  Var
    a,b: SemCasaDecimal;

:)
  SERA_QUE a > b ?
    :)
      a <- 4;
      b <- a + 2;
    :(;
  
  WriteLn( 'hello, world' );
  Writeln( 3 );
:(.

